from setuptools import setup, find_packages

setup(
    name = "src",
    version = "0.0.1",
    description = "its a email phishing package",
    author = "sumit",
    packages = find_packages(),
    license = "MIT"
)